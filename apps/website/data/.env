INFLUX_ORG=master
DATA_BUCKET=rawdata
LOG_FILE_PATH=/Users/zoe/Documents/github/viot/auto_depoly/viot/var/log/main.log # Log 寫入檔案路徑 - 建議依環境修改
DEFAULT_TIMEOUT="5s"                      # 預設超時時間（秒）- 建議使用預設值
SET_REPLACE_PAUSE_AFTER_CONNECT="500ms"      # 每次連線後等待時間 - 建議使用預設值
DEFAULT_ENABLED="1"                      # 預設是否啟用設備條目 - 建議使用預設值
DEFAULT_RETRIES=3                      # 預設重試次數 - 建議使用預設值

INFLUX_URL=http://localhost:8888
INFLUX_TOKEN=4-Z-WuwUTh74YXnGleK4Oab7Re86bpwBz-JFXLIl86BtYDt1RAMuNUkTT0e_MKftdqedZxDZX-_kv35KnB03ng==
LOG_ROTATE_BY_DAY=true                   # 是否每日分檔（true/false）- 建議使用預設值
SET_MODBUS_PORTS="7000:7001"        # 預設掃描 port 列表 - 建議使用預設值
SET_MODBUS_SLAVES="0:1:2"          # 預設掃描 slave ID 列表 - 建議使用預設值
DEFAULT_SNMP_VERSION="2c"                # SNMP 預設版本 - 建議使用預設值
BASE_DIR=/Users/zoe/Documents/github/viot/auto_depoly          # 預設專案根目錄 - 不建議修改，除非啟動器未正確傳入
MONITOR_BUCKET=monitor
LOG_LEVEL=DEBUG                            # Log 顯示層級（DEBUG, INFO, WARN, ERROR）- 建議使用預設值
LOG_FILE_LEVEL=DEBUG                     # 寫入 log 檔的最低層級 - 建議使用預設值
PDU_NAME_FORMAT='${factory}${phase}${dc}${room}${rack}P${side}'
SET_REPLACE_BUSY_RETRIES_WAIT="500ms"        # Busy 設備等待時間 - 建議使用預設值
SET_REPLACE_PAUSE_BETWEEN_REQUESTS="1000ms"  # 請求間 pause - 建議使用預設值
DEFAULT_SNMP_COMMUNITY="public"          # 預設 SNMP community - 建議依環境修改
DEFAULT_INTERVAL="60s"                    # 預設採集間隔（秒）- 建議使用預設值

USERNAME=admin
PASSWORD=admin
COOKIE_MAX_AGE=3600
STATUS_COLOR_0=#a5dfa5
STATUS_COLOR_1=#ffff66
STATUS_COLOR_2=#ea4335
STATUS_COLOR_DEFAULT=#ddd
STATUS_TEXT_COLOR_DEFAULT=#000000
STATUS_COLOR_EMPTY=#ddd
STATUS_TEXT_COLOR_EMPTY=#ddd

AGENT_INTERVAL=60